# PR #39 리뷰 — 리워드광고 사용자 앱: 캠페인 생성·내역·공유 팩·재청구 (P0-4)

- 리뷰: 강체크 (qa) · 2026-09-08
- 대상: `claude/reward-user-ui` → base `claude/reward-ledger-fix`, 커밋 `e4fd8e2`, 18 files / +2,017 −451
- 방법: 워크트리 읽기 전용 검토 + `npm test`(31/31) + `npm run build` 직접 실행. 브리프 필수 확인 8개 전부 판정.

## 판정: **승인** — 차단 0. 원장 트랜잭션 무변경, 금칙어 서버 관문 존재, 소유 검증·XSS·데모 게이트 통과. 비차단 6건.

---

## 1. 차단 항목
없음.

## 2. 비차단 (후속 PR / 기록)

| # | 위치 | 내용 | 심각도 | 우회안 |
|---|---|---|---|---|
| N-1 | `lib/reward-copy-templates.ts:196-247` `findForbiddenTerms` | `normalize("NFC")`만 하고 **공백·전각·제로폭 정규화 없음** → `1 2 0 %`, `120 %`, `１２０％`(전각), `수 익`, `12 만`, `백이십 퍼센트`가 전부 통과. 관리자 승인이 최종 관문이라 원장 리스크는 0이나, 아뱅 §1.5 취지("제출 전에 막아 반려 건수 감소")가 반쯤만 산다 | 주의 | 검사 전 `normalize("NFKC")` + `/[\s​-‍﻿]/g` 제거한 사본으로 매칭. 규칙 #1·#2에 `백이십`, `120퍼`, `1\.2배` 변형 추가. 테스트 3줄 |
| N-2 | `api/reward/redeem/route.ts:13` `TERMINAL_CODES` | `CAMPAIGN_NOT_ACTIVE`를 "일시정지 → 재개 가능"으로 보고 남기는데, 같은 코드가 **`ended`·`rejected`(영구)** 에서도 나온다(`canRedeem` false 전부). 그러면 `pendingRewardCode`가 영원히 남아 대시보드 배너가 "지급이 중단되었습니다"로 **영구 노출**(`PendingRewardBanner:39, 123`은 버튼만 숨김) | 주의 (UX) | 서버는 이미 `details.status`를 준다(`reward-service.ts:347-349`) → 라우트에서 `status ∈ {ended, rejected}`면 지움. `paused`·`pending_review`만 남김 |
| N-3 | `reward-service.ts:766` `inviteeMasked = uid 앞 4자 + ****` | Firebase uid 28자 무작위라 4자로 식별은 불가. 다만 광고주에게 필요한 정보는 "몇 명·언제·얼마"뿐이고 uid 조각은 캠페인 간 교차 대조 재료가 된다(최소수집 원칙) | 경미 | `#1, #2…` 순번 + `paidAt`만. `id`는 클라이언트 key 용도라 유지 |
| N-4 | 정직줄 강제 범위 | 정직줄은 **클라이언트에서만** 붙는다: 생성 미리보기(`CampaignCreateForm:309`), 공유 팩 4종(`buildSharePack` 끝 줄), 상세(`CampaignDetail:209`). 서버 `copy`에는 없고 관리자 승인 드로어(`admin/reward/page.tsx:349-354`)도 정직줄 없이 헤드라인·본문만 보여준다. 아뱅 문서 요구("공유물·상세 끝")는 충족. 광고주가 복사한 텍스트에서 손으로 지우는 건 어떤 구현도 못 막고, 받는 쪽 가입 화면이 "광고주 예산에서 N P 지급"을 다시 말하니 실피해 없음 | 경미 | 관리자 드로어 미리보기에도 `honestLine(c.code)` 한 줄(검토자가 보는 것 = 회원이 보는 것) |
| N-5 | `deposit/page.tsx:64` `if (!isConfigured \|\| !db)` | `isConfigured`면 실연동 경로라 데모 분기 안 탄다 ✓. 단 `isConfigured && !db`(초기화 실패)일 때 **localStorage에 가짜 입금**이 저장되고 사용자는 성공 화면을 본다 | 경미 | 그 조합은 `setError("연결 오류")`로. `getBalance` 분기(`:39`)도 동일 |
| N-6 | `dashboard/page.tsx:36` `invite_advertiser: "초대 수익"` | 감사 1.10 "치환" 항목이 아직 그대로(이 PR 범위 밖, 기존 코드). 베타 거래 라벨이라 노출은 낮음 | 경미 | "초대 리워드 (베타 조정)" — 다음 문구 PR에 |

## 3. 필수 확인 8개 — 판정표

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | **원장 무변경 · 금칙어** | 통과 | `git diff -- reward-service.ts`: `createCampaign`은 캠페인 문서에 `templateId`·`ownerName` 2필드 추가(`:316-319`)뿐, 잔액 읽기·검사·`tx.update/create` 순서 무변경. `redeemCampaignCode`·`applyAdminAction`·`cancelCampaign` diff 0. 신규 함수는 `listPayoutsForOwner`·`clearPendingRewardCode`(원장 밖 `set merge`). 금칙어 11규칙 = 아뱅 §1.5 #1~#11 항목별 일치(#7 "확정 수익"은 "수익" 규칙에 포함), #12 타사 상호는 문서대로 `findBrandWarnings` 경고만. `validateCreateInput`이 `copy`·`ownerName` 둘 다 서버에서 검사(`:208-219`). 우회 가능성은 N-1 |
| 2 | **`pendingRewardCode` 정리 기준** | 조건부 | `ALREADY_REDEEMED`·`SELF_INVITE`·`NOT_FOUND`(캠페인 문서는 삭제되지 않으므로 = 애초에 없는 코드, 영구) 지우는 것 맞다. `BUDGET_EXHAUSTED`: 예산은 **충전이 없어** 한 캠페인에서 영구 — 지우는 것 맞다. `DAILY_CAP_REACHED`(429)는 `TERMINAL_CODES` 밖이라 구분됨 ✓, 배너도 "내일 다시" 안내(`:99-100`). 빠진 건 `CAMPAIGN_NOT_ACTIVE`의 영구 상태(N-2) |
| 3 | **payouts 소유·마스킹** | 통과 | `listPayoutsForOwner:756-761` 캠페인 문서 읽고 `ownerUid !== uid` → 403, 그 다음에야 `rewardPayouts` 조회. 라우트는 `requireAuth` + `id.toUpperCase()` + `assertCampaignId` 형식 검사. 마스킹은 N-3 |
| 4 | **`?code=` 프리필** | 통과 | `page.tsx:22` `searchParams.get("code")`(구 `invite`도) → `trim().toUpperCase()` → `:41` `/^[A-Z0-9]{8}$/` 불일치면 `INVALID_FORMAT` 배너, API 호출 안 함. API 호출은 `encodeURIComponent`(`:47`). 화면 출력은 JSX 텍스트(`가입 코드 적용됨 · {inviteCode}`) → 이스케이프. **리다이렉트 파라미터 없음**(가입 후 `router.push("/dashboard")` 고정 `:64`). `localStorage`/`sessionStorage` 저장 0건(grep). 코드는 post-signup 요청 본문으로만 |
| 5 | **정직줄 강제** | 조건부 | 클라이언트 세 곳 전부 자동 부착, 광고주 편집 불가 영역. 서버 저장 `copy`에는 없음 — 아뱅 문서는 "공유물·상세"만 요구하므로 규격 충족. N-4 |
| 6 | **재발송 쿨다운 · 토큰 갱신** | 통과 | 쿨다운 60초는 **클라이언트만**(`PendingRewardBanner:8, 60`) — 새로고침하면 리셋되지만 Firebase가 `auth/too-many-requests`로 자체 제한하고 그 코드를 잡아 안내(`:63-67`). 서버 부하 없음(직접 Firebase 호출). 인증 반영: `claim()`이 `u.reload()` → `emailVerified` 확인 → **`getIdToken(true)`**(`:80-87`) → 그 뒤 `/api/reward/redeem`. 순서 정확. 서버 게이트가 `EMAIL_NOT_VERIFIED`를 또 내면 "잠시 후 다시" 안내(`:97-98`) |
| 7 | **데모 `deposit` 게이트** | 통과 | `deposit/page.tsx:64` `!isConfigured \|\| !db`일 때만 `saveDemoDeposit`. 실연동(`isConfigured && db`)은 기존 `/api/deposit` 경로. `dashboard:~` 배너도 `isConfigured &&`로 감싸 데모에서 미표시. 엣지 N-5 |
| 8 | **정직 문구** | 통과 | 새 화면(`advertiser/*`, `components/reward/*`, 템플릿) grep: "120%·수익·평생·5%·투자·데이터노동·순증·돌아옵니다" 매치는 금칙어 정의부와 "남은 예산은 잔액으로 돌아옵니다"(반환, 사실)뿐. 광고주 이득 표기는 "광고주가 얻는 것: 회원 N명"만. `dashboard:214, 281`의 "120% 적립"은 카드 지출 문맥(기존, PR #34 정합). 구 초대 화면은 `/advertiser`로 `router.replace`, 공유 문구는 템플릿 ① 카톡 1줄로 교체됨 |

---

## 4. 테스트 실행 원문

워크트리 `.claude/worktrees/agent-ac12a743fac3535c5/app`, 2026-09-08 09:13 KST.

```
> app@0.2.0 test
> vitest run

 ✓ src/lib/__tests__/reward-ledger.test.ts (9 tests) 20ms
 ✓ src/lib/__tests__/reward-copy-templates.test.ts (7 tests) 23ms
 ✓ src/lib/server/__tests__/reward-service.test.ts (15 tests) 43ms

 Test Files  3 passed (3)
      Tests  31 passed (31)
   Start at  09:13:41
   Duration  3.86s
```

`npm run build`:

```
✓ Compiled successfully in 9.7s
✓ Generating static pages using 11 workers (46/46) in 510.4ms
```

타입·빌드 오류 0. 신규 라우트 `/api/reward/campaigns/[id]/payouts` 포함, 페이지 수는 PR #37 기준과 동일(46) — 구 `/advertiser/invite`는 리다이렉트 화면으로 남음.

커버리지 메모: 신규 7건은 전부 `reward-copy-templates`(템플릿 렌더·정직줄·금칙어·공유 팩)이고, 원장 15건은 **무변경 통과** — §3-1의 "원장 diff 0"을 테스트가 한 번 더 뒷받침한다. 화면 3종(`CampaignCreateForm`·`CampaignDetail`·`PendingRewardBanner`)은 단위 테스트 없음 → 스테이징 수동 회귀 필요:
1. 자격 미달(입금 < 10만) 계정에서 캠페인 생성 시도 → 403 안내 + 입금 유도
2. 캠페인 제출 → 잔액 −예산 / 잠김 +예산 → 승인 전 취소 → 전액 복구
3. 공유 팩 3종 복사 → 붙여넣은 텍스트 끝에 정직줄 포함 확인
4. `?code=` 링크로 가입 → 미인증 상태 배너 → 인증 → [리워드 받기] → 잔액 반영·배너 소멸
5. 데모 모드(`isConfigured=false`)에서 입금 → 캠페인 생성까지 localStorage로 완주

## 5. 정성 평가 한 줄
금칙어를 클라이언트와 서버 양쪽에 두고 정직줄을 코드가 붙이게 한 게 이 PR의 핵심 — 광고주가 문구를 자유롭게 쓰되 원장이 말하지 않는 약속은 저장 단계에서 걸린다. 남은 구멍은 공백·전각 우회(N-1)뿐이고, 그건 관리자 승인이 마지막으로 잡는다.

## 6. 사고 대응 한 줄
금칙어를 우회한 문구가 승인·송출됐다면 → 해당 캠페인 `pause` → `copy` 문제 문구 확인 후 `end`(잔여 반환) → `FORBIDDEN_RULES`에 변형 패턴 추가 + 같은 패턴 전수 재검색(`rewardCampaigns.copy`).

검토 결과: 통과 가능
