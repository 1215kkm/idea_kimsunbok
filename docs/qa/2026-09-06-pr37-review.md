# PR #37 리뷰 — 리워드 원장 제로섬 전환 (`claude/reward-ledger-fix` → `main`)

- 리뷰: 강체크 (qa) · 2026-09-06
- 기준: `docs/qa/2026-09-06-honest-copy-audit.md` §4 체크리스트 (필수 9 · 권장 11 · 회귀 3), 기획서 §2·§4.2·§4.3·§8
- 방법: 워크트리 읽기 전용 검토 + `npm test` (vitest 24건) + `npm run build` 직접 실행. 커밋 `e8be044` · `99cd0cd` · `4a29d17`, 30 files / +4,662 −961.

## 판정: **조건부 승인** — 차단 2건(문구·UX 정직성, 합쳐 30줄 안팎)만 이 PR에서 고치면 머지. 원장 자체는 필수 9개 전부 통과.

---

## 1. 차단 항목 (머지 전 필수)

### B-1. 가입 화면이 지급을 약속하는데 이 PR 이후 실환경에선 100% 실패한다 — `app/src/app/page.tsx:146-151, 97-105`
- 사실: `post-signup/route.ts:62-65`는 가입 직후 `user.emailVerified`(그 시점엔 항상 `false`)로 `redeemCampaignCode`를 호출 → 항상 `EMAIL_NOT_VERIFIED`. 가입 화면은 `:148` "회원가입을 완료하면 N P가 **자동으로 지급됩니다!**"를 띄우고, 에러 매핑 `:97-105`에는 `EMAIL_NOT_VERIFIED`가 없어 **아무 안내 없이 조용히 끝난다.** 코드도 저장하지 않아 인증 후 청구할 길이 없다(청구 UI 자체도 없음).
- 왜 차단: 감사표 1.11 "치환" 대상이 이 PR로 **허위**가 된다. 원장은 정직해졌는데 화면이 거짓말하는 상태로 main에 올라간다.
- 요구 (약 20줄):
  1. `page.tsx:146-151` 배너 → "가입 후 **이메일 인증을 마치면** 광고주 예산에서 N P가 지급됩니다".
  2. `page.tsx:97-105` `EMAIL_NOT_VERIFIED` → "이메일 인증 후 지급됩니다. 받은 메일의 링크를 눌러 주세요." 매핑 추가.
  3. `post-signup/route.ts` catch 블록에서 `EMAIL_NOT_VERIFIED`면 `users.pendingRewardCode = inviteCode` 저장 (P0-4가 인증 후 자동 청구할 근거). `AuthContext.signUp`에 `sendEmailVerification(cred.user)` 한 줄도 같이 넣으면 P0-4 전에도 죽은 기능이 아니게 된다 — 권장이지 차단 아님.

### B-2. 리워드 초대 화면이 죽은 링크를 공유시키고, 410을 "발급 실패"로 뭉갠다 — `app/src/app/advertiser/invite/page.tsx:72-94, 239-268`
- 사실: `GET /api/invite/code`가 구 `inviteCodes` 활성 코드를 그대로 돌려주고(`invite/code/route.ts:13-33` 유지), 화면은 "현재 활성 코드" + **링크 복사 / 공유하기** 버튼을 그대로 보여준다. 공유 문구는 이 PR에서 "광고주 예산에서 지급"으로 바뀌었는데 그 코드는 `invite/info`에서 410 `INACTIVE`, 리딤은 410 `INVITE_DEPRECATED`. 광고주가 SNS에 뿌린 링크는 전부 "구 초대 코드는 더 이상 사용할 수 없습니다"로 떨어진다. 새 코드 발급(`issueCode` → `POST /api/invite/code`)은 410인데 `:89` "초대 코드 발급에 실패했습니다."로만 보인다.
- 요구 (약 10줄): 활성 구 코드가 있으면 "이 코드는 지급이 중단되었습니다 (베타 초대). 캠페인 제출로 대체됩니다" 배지 + 복사·공유 버튼 비활성. `issueCode` catch에서 `ApiClientError.code === "INVITE_DEPRECATED"`면 그 메시지 표시. P0-4에서 화면을 갈아엎더라도, 그 사이 main이 죽은 링크 배포기가 되면 안 된다.

---

## 2. 비차단 (후속 PR / 기록)

### 2.1 강개발이 밝힌 확인 지점 5개 — 판정

| # | 강개발 주장 | 판정 | 근거 · 조치 |
|---|---|---|---|
| 1 | 이메일 인증 게이트가 실환경 지급을 전부 막는다. P0-4 전까지 리워드는 사실상 죽은 상태 | **맞다, 그리고 그게 정직하다.** 단 "죽은 상태"가 **보여야** 한다 → B-1 | 무에서 생성을 열어 두는 것보다 닫힌 게 낫다. `AuthContext.tsx:78-81`에 `sendEmailVerification` 한 줄 + `pendingRewardCode` 저장이면 P0-4 없이도 "인증 → 재로그인 → 자동 청구" 경로가 된다 (권장) |
| 2 | 광고주 자격(입금 ≥ 10만)이 베타 테스터 전원을 막는다 | **틀렸다.** 막지 않는다 | `api/deposit/route.ts:36` `method = body.method ?? "beta_virtual"`, `:58-65` `type:"deposit"` 거래 생성. `getConfirmedDepositTotal`(`reward-service.ts:205-217`)은 method를 안 본다 → `/deposit`에서 가상 10만 넣으면 통과. 테스트 `seedDeposit`(`:79-81`)도 `method:"beta_virtual"`로 통과 중. 베타에선 이대로 OK. **P1-2 입금확인 도입 시 `method !== "beta_virtual"` + `status:"confirmed"` 필터 필수** — 안 하면 정식 환경에서 자격 게이트가 무의미 |
| 3 | `ledger/totals`의 `untyped_legacy` — type 없는 거래를 spend로 취급 | **수용** | 서버 코드에 type 없는 거래를 만드는 곳 없음(grep 0, 초기 클라이언트 직접 쓰기 잔재). `dashboard/page.tsx:97`·`history/page.tsx:104`가 이미 `!type`을 spend로 취급하므로 총량 API가 화면과 같은 해석. `byType.untyped_legacy.count`로 노출되니 감춰진 것도 아님. 후속: count > 0이면 `warnings`에 한 줄 |
| 4 | `card-service` 카드 실지출이 원금 차감 없이 +120% → `spend_card` 별도 집계. 무에서 생성의 정식 경로인가 | **정식 경로 아니다 — 앱 문구와 모순. 원장 정책 결정 필요, 이 PR 밖** | `card-service.ts:185-186` `cur + nl.totalAccumulation`, 차감 없음 → 회원 순 +120%. 포인트 결제 `spend/register:70`은 `−amount + 120%` = 순 +20%. 앱 문구 `stores/page.tsx:231` "본인 충전데이터에서 지출금액이 **차감**됩니다", `deposit/page.tsx:201-202` "**차감하여** 이체 → 120% 재충전"은 차감을 약속. 기획서 §2.1(b) "카드 실지출 OCR 후 120%"는 문자 그대로면 허용이지만 플랫폼 부채가 포인트 경로의 **6배**(20% vs 120%). 결정 (CEO/강팀장): **(A)** 카드 동기화 시 잔액 ≥ 금액이면 원금 차감 후 120% (문구와 정합, 추천) / **(B)** 카드 = +120% 확정하고 문구를 고침. 그 전까지 totals가 `spend_card`를 `warnings`에 "정책 미확정 발행"으로 띄우길 권장 |
| 5 | 데모 모드에 인증·자격 게이트 미러링 없음 | **수용 (지금은 영향 0)** | `demo-store.createCampaign/redeemCampaign/setCampaignStatus` 호출처 0건(grep). P0-4가 데모 UI를 붙일 때: 이메일 게이트는 데모에 인증이 없으니 생략 가능, **자격(입금 10만)은 미러링** — 실환경과 데모가 다르게 동작하면 테스터가 실환경에서 403을 보고 버그로 신고한다. 제로섬 규칙(잠금→이전→반환)은 `reward-ledger` 순수 함수를 공유해서 이미 같음 |

### 2.2 기획서와 다른 결정 3개 — 전부 수용

| 결정 | 판정 | 조건 |
|---|---|---|
| 캠페인 문서 ID = 8자 코드 | **수용** | `tx.create` 충돌 시 재시도(`reward-service.ts:242-298`)로 유일성 보장, `info` 조회 O(1). 열거 공간 32^8 ≈ 1.1×10^12 — 비인증 `invite/info`가 `amount·status`만 노출하고 `ownerUid`는 안 나감(`getCampaignPublicInfo:700-710`) |
| `approve` → `live` 직행 | **수용 (P0)** | P0에 송출 단계가 없으니 맞다. `approved`는 `canRedeem`·`pause.from`에 남아 있어 죽은 상태로 무해. P1-5 성과/송출 붙일 때 `start` 액션을 추가하고 그때 `approve → approved`로 되돌릴 것 — 지금 `ADMIN_TRANSITIONS` 한 줄 바꾸면 됨 |
| 본인 취소 = `rejected` + `rejectReason:"owner_cancelled"` | **수용** | 관리자 목록 `status=rejected` 필터에 관리자 거절과 섞인다 → 관리자 화면(P0-3)에서 `rejectReason`을 컬럼으로. 감사 로그는 없지만 `reward_refund{reason:"owner_cancel"}` 거래가 남아 추적 가능. `draft`도 허용하는데 `draft`를 만드는 코드가 없음 — 무해 |

### 2.3 추가로 보라고 한 5개

| 항목 | 결과 | 근거 |
|---|---|---|
| 이메일 정규화 해시 | **통과** | `reward-ledger.ts:45-58`: `lastIndexOf("@")` 분리 후 **`gmail.com`/`googlemail.com`에서만** 점·`+태그` 제거. 다른 도메인은 소문자화만 (`first.last+x@example.com` 유지, 테스트 `:76`). 오적용 없음. 참고: outlook/hotmail/live도 `+` 별칭을 지원 → 후속에서 `+` 제거 도메인 목록에 추가하면 방어 폭이 넓어짐 (선택) |
| `withAdminAudit` 실패 시 로그 | **통과 (남기지 않음)** | `reward-service.ts:516-528` 감사 문서 `tx.create`가 `runTransaction` **안**에서 마지막 쓰기 → `fn` throw 시 트랜잭션 전체 abort, 로그 0. 테스트 (f) `:242-244` reason 없는 reject 400 후 `adminAuditLogs` 1건(성공분만) 확인. 실패 시도 기록이 필요하면 별도 `adminAuditAttempts` (P1, 선택) |
| `firestore.rules` 차단 | **통과** | `app/firestore.rules` (+27): `rewardPayoutKeys` read/write false, `adminAuditLogs` read/write false, `rewardCampaigns` 본인 읽기, `rewardPayouts` 본인·광고주 읽기. `firebase.json`은 `app/`에 있어 이 파일이 배포본 |
| `dailyCap` 일 기준 시간대 | **통과 (KST)** | `seoulDateKey` `Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Seoul"})` → `YYYY-MM-DD`. 테스트 `reward-ledger.test.ts:80-83` UTC 14:59:59 → 09-06, 15:00:00 → 09-07. 서비스 테스트 `:304-328` 자정 경계 리셋 확인. Vercel Node는 full-ICU라 `Asia/Seoul` 안전 |
| 총량 API 전체 스캔 | **주의 — 지금은 OK, 회원 1만/거래 10만 넘기 전에 교체** | `ledger/totals/route.ts:32, 46-49` `users`·`transactions` 전건 `get()`. Firestore 읽기 ~1~2만 문서/초, Vercel 함수 제한 10s(Hobby)/60s(Pro) → 거래 10만 건이면 Hobby에서 타임아웃, 메모리도 문서당 수백 B × 전건. 교체안: (1) Admin SDK `AggregateField.sum("totalPoints")` 집계 쿼리 — 클라이언트 전송 없이 서버 집계, users 좌변은 이걸로 즉시 O(1) / (2) 거래 우변은 P1-9 일별 스냅샷(증분) / (3) 임시로 `?limit`+커서 페이지네이션. 지금 회원 수에선 문제 없음 |

### 2.4 그 밖에 발견 (후속)

| # | 위치 | 내용 | 심각도 |
|---|---|---|---|
| N-1 | `history/page.tsx:96-101` | `reward_refund`를 `earned`(적립)에 더한다. 반환은 내 돈이 돌아온 것이지 적립이 아님 → 광고주 "총 적립"이 부풀려 보임 | 주의 (정직 표기) |
| N-2 | `dashboard/page.tsx:226-230` | `reward_out`을 `-amount`로 표시. 실제 `totalPoints` 변화는 0(잠금에서 나감). 광고주 눈엔 `reward_lock -30만` 다음 `reward_out -10만`이 연달아 → 이중 차감으로 오해. history 라벨엔 "(잠금 예산에서)"가 있는데 dashboard 라벨엔 없음 | 경미 |
| N-3 | `reward-service.ts:387-391` | 회원 문서 없으면 `set merge`로 `{totalPoints, lockedPoints, createdAt}`만 생성 → `name/email/role` 없는 유령 문서. `post-signup`이 먼저 만들어 실제론 안 생기지만 `/api/reward/redeem` 직접 호출 시 가능 | 경미 |
| N-4 | `api/invite/code/route.ts:26` (GET 유지분) | 응답에 `totalAdvertiserNetGain` 여전히 노출. 화면은 더 이상 안 쓰지만 체크리스트 19 "응답 JSON 어디에도 advertiserNetGain 없음" 위반 | 경미 — B-2 손볼 때 같이 제거 |
| N-5 | 체크리스트 15 | 지급 역전(`reversed`) 미구현. `rewardPayouts.status`는 항상 `"paid"`. 어뷰징 발견 시 되돌릴 도구 없음 | 주의 — P0-3 관리자 화면과 같이 |
| N-6 | 체크리스트 18 | `admin/users/route.ts` 응답에 `lockedPoints` 없음 (변경 안 됨) → 관리자가 광고주 잔액을 실제보다 적게 본다 | 경미 — P0-6 이관 때 |
| N-7 | `docs/migrations/2026-09-reward-ledger.md:18` | `inviteCodes.active`를 false로 안 뒤집음("스크립트 불필요"). 리딤 경로가 사라져 실질 무해하나 GET이 "활성"으로 돌려줘 B-2를 만든다. 뒤집는 배치 1회가 더 깔끔 | 경미 |
| N-8 | `reward-service.ts:231` | 자격 검사가 트랜잭션 밖 — 입금 합계는 줄지 않으니 맞는 판단. 단 P1-2에서 "입금 반려"가 생기면 줄 수 있음 → 그때 재검토 | 기록 |

---

## 3. 체크리스트 통과표 (`honest-copy-audit.md` §4)

### 필수 9 — 전부 통과

| # | 항목 | 결과 | 증거 |
|---|---|---|---|
| 1 | 제로섬 골든 (생성→승인→리딤 2→종료, Σ 불변, 거래 6종) | 통과 | `reward-service.test.ts` (a) `:100-168` — `ledgerTotal()` 5회 불변, types `[in,in,lock,out,out,refund]`, 광고주 1,000,000 → 800,000 |
| 2 | 멱등 `tx.create` (set merge 금지) | 통과 | `reward-service.ts:426, 438` `tx.create(payoutRef/emailKeyRef)`; 테스트 (c) `:184-212` 2회째 409 |
| 3 | 잠금이 실제 차감 — 출금·지출 못 씀 | 통과 | `createCampaign:261-263` `totalPoints −budget`; 테스트 #7 `:364-375` `withdraw-service` 무수정 상태로 800,000·700,001 → `INSUFFICIENT_BALANCE`, 700,000 OK |
| 4 | 잔액 부족 제출 → 아무 문서 없음 | 통과 | 테스트 (b) `:170-182` 409, `rewardCampaigns` 0건, 비-deposit 거래 0건 |
| 5 | 자기 초대 400 | 통과 | `reward-service.ts:343-345`; 테스트 (d) `:214-219` |
| 6 | 구형 경로 폐쇄 | 통과 | `api/invite/redeem/route.ts` 410 `INVITE_DEPRECATED`; `invite-service.redeemInviteCode` 즉시 throw 410; `POST /api/invite/code` 410; `invite/info` 구 코드 410 `INACTIVE`. `inviteCodes.active` 미반전은 N-7 |
| 7 | 베타 `inviteRedemptions/{uid}` 차단 | 통과 | `reward-service.ts:325, 334, 338`; 테스트 (c) `:205-208` |
| 8 | 동시성 — 잔여 1건에 2명 → 1명만 | 통과 | 테스트 #8 `:377-400` `Promise.allSettled`, `contentionRetries > 0` 확인(가짜 Firestore가 낙관적 잠금·ABORTED 재시도·"쓰기 후 읽기 금지"를 재현, `fake-firestore.ts:95-143`). 실 Firestore와 동일 원리이나 **실환경 재현은 아님** — 배포 후 스테이징에서 1회 확인 권장 |
| 9 | 이메일 인증 게이트 | 통과 (원장) / **차단 B-1** (화면) | `reward-service.ts:315-317`; `reward/redeem/route.ts:19-22`·`post-signup:62-65`가 `user.emailVerified` 실제 전달; 테스트 `:294-302` |

### 권장 11

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| 10 | 거절·종료·취소 status 가드 (이중 반환 방지) | 통과 | `nextStatus` 전이표 + 테스트 (f) `:262-264`, (g) `:286-288`, #6 `:355-356` |
| 11 | `cancel` (draft/pending_review만) | 통과 | `reward-service.ts:636-670`, 라우트 `reward/campaigns/[id]/cancel` |
| 12 | 일일 상한 + 이메일 정규화 키 | 통과 | `DEFAULT_DAILY_CAP=10`, 관리자 `daily-cap` 액션, `rewardPayoutKeys/{sha256}`; 테스트 `:304-328` |
| 13 | `adminAuditLogs` before/after, reject·end reason 필수 | 통과 | `withAdminAudit:508-529`, `REASON_REQUIRED_ACTIONS`; 테스트 (a) `:154-164`, (f) |
| 14 | rules 4개 컬렉션 | 통과 | `app/firestore.rules` diff |
| 15 | `reversed` 부분 회수·`shortfall` | **미구현** | N-5. P0-3 관리자 화면과 함께 |
| 16 | 화면 라벨 `reward_*` | 통과 | `dashboard:36-39`, `history:39-42`. N-1·N-2 표시 정확성은 후속 |
| 17 | 총량 정합식에 베타 시드·베타 조정 | 통과 | `ledger/totals:43, 83-88, 104-108` `betaInitialFunds` + `betaAdjustment` |
| 18 | `admin/users`에 `lockedPoints` | **미반영** | N-6 |
| 19 | 응답 JSON에 `advertiserNetGain` 없음 | **부분** | `invite/code` GET `:26` `totalAdvertiserNetGain` 잔존 (N-4). 신규 라우트는 전부 깨끗 |
| 20 | `npm run build` + 데모 모드 안전 | 통과 | 빌드 원문 §4. 데모: 캠페인 함수 호출처 0 → 기존 데모 배너 그대로 |

### 회귀 3

| 항목 | 결과 | 방법 |
|---|---|---|
| 입금→지출→출금 요청→취소 골든 | 통과 (코드 검토) | `deposit`·`spend/register`·`withdraw-service` **diff 없음**. 출금 테스트 #7이 `requestWithdrawal` 실제 호출로 잔액 차감 확인. 브라우저 클릭 회귀는 vitest 범위 밖 — 스테이징에서 1회 |
| 관리자 출금 승인·반려 | 통과 (코드 검토) | `admin/withdraw/*` diff 없음 |
| 코드 없는 일반 가입 → 베타 시드만 | 통과 (코드 검토) | `post-signup:41-56` 무변경, `inviteCode` null이면 리딤 분기 진입 안 함 |

---

## 4. 테스트 실행 원문

워크트리 `.claude/worktrees/agent-ab4bbe2d0a194e86a/app`, 2026-09-06 08:46 KST.

```
> app@0.2.0 test
> vitest run

 RUN  v3.2.7 C:/Users/강경모/idea_kimsunbok/.claude/worktrees/agent-ab4bbe2d0a194e86a/app

 ✓ src/lib/__tests__/reward-ledger.test.ts (9 tests) 17ms
 ✓ src/lib/server/__tests__/reward-service.test.ts (15 tests) 37ms

 Test Files  2 passed (2)
      Tests  24 passed (24)
   Start at  08:46:43
   Duration  732ms (transform 157ms, setup 0ms, collect 258ms, tests 54ms, environment 0, prepare 368ms)
```

`npm run build` (tail):

```
├ ƒ /api/invite/code
├ ƒ /api/invite/info
├ ƒ /api/invite/redeem
├ ƒ /api/reward/campaigns
├ ƒ /api/reward/campaigns/[id]/cancel
├ ƒ /api/reward/redeem
├ ƒ /api/spend/register
├ ƒ /api/withdraw/cancel
├ ƒ /api/withdraw/list
├ ƒ /api/withdraw/request
... (○ Static 페이지 목록 생략)
○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand
```
빌드 오류·타입 오류 0. `admin/reward/*`·`admin/ledger/totals` 라우트는 목록 상단(알파벳순)에 포함.

---

## 5. 정성 평가 한 줄
원장은 내가 §2에 적은 옆문 네 개(구형 라우트·인증 없는 지급·다른 컬렉션 멱등 키·감사 로그 0)를 전부 닫았고, 가짜 Firestore가 "쓰기 후 읽기 금지"까지 재현해서 실환경 제약을 테스트가 붙잡고 있다 — 남은 건 화면이 원장만큼 정직해지는 것 30줄이다.

## 6. 사고 대응 한 줄
배포 직후 `GET /api/admin/ledger/totals`에서 `diff ≠ 0` 또는 `rewardNet ≠ 0`이면 → 진행 중 캠페인 전부 `end`(잔여 반환) → `adminAuditLogs`·`transactions(reward_*)`로 재생(replay) → 원인 좁힌 뒤 재개.

검토 결과: 조건부 통과

---

## 재검토 (a655cec)

`npm test` 24/24 통과 (08:57:25, 894ms) · `npm run build` "Compiled successfully in 6.0s", 46/46 정적 페이지.

| 항목 | 요구 | 확인 | 판정 |
|---|---|---|---|
| B-1 배너 | "이메일 인증 후 지급" | `page.tsx:152-157` "가입 후 이메일 인증을 마치면 광고주 예산에서 N P가 지급됩니다." (inviteInfo 없을 때도 동일 톤) | 충족 |
| B-1 에러 매핑 | `EMAIL_NOT_VERIFIED` | `page.tsx:106-111` + `CAMPAIGN_NOT_ACTIVE/BUDGET_EXHAUSTED`·`INVITE_DEPRECATED`까지 | 충족 (요구 초과) |
| B-1 코드 보관 | `users.pendingRewardCode` | `post-signup:71-74` `EMAIL_NOT_VERIFIED`일 때만 `set merge` — userRef는 직전에 생성/존재 보장 | 충족 |
| B-1 인증 메일 | `sendEmailVerification` | `AuthContext:81-86` try/catch, 실패는 `console.error`만 하고 가입·post-signup 계속. 데모 분기(`!isConfigured` 조기 return)는 이 호출 **앞**이라 데모 모드 영향 없음 | 충족 |
| B-2 배지·버튼 | 지급 중단 배지 + 복사·공유 비활성 | `invite/page.tsx:215-235` amber 배지 "지급 중단 (베타 초대)", 코드 취소선·opacity 60, 두 버튼 `disabled` + `cursor-not-allowed`. 죽은 `handleCopy/handleShare/copied` 제거 | 충족 |
| B-2 410 메시지 | `INVITE_DEPRECATED` | `invite/page.tsx:87-92` `ApiClientError.code` 분기 → 서버 메시지 그대로 | 충족 |
| 비차단 덤 | totals warnings | `untyped_legacy` 건수, `spend_card` "정책 미확정 발행" 경고 추가 | 좋음 |

새로 생긴 문제: **차단급 없음.** 기록만.
- `pendingRewardCode`를 지우는 코드가 없다 — 인증 후 청구 성공 시 P0-4가 `FieldValue.delete()`로 정리할 것. 지금은 읽는 곳도 없어 무해.
- 가입 성공인데 `EMAIL_NOT_VERIFIED` 안내가 빨간 `setError` 스타일로 뜨고 `useEffect` 라우팅으로 곧 사라진다 — 기존 `SELF_INVITE` 등과 같은 패턴. P0-4에서 대시보드 상단 "지급 대기 N P" 배너로 옮기면 사용자가 놓치지 않는다.
- B-2 카드에 `inviteUrl` 텍스트는 여전히 보인다(opacity 60). 손으로 복사해 뿌려도 받는 쪽은 410 정직 메시지라 피해 없음. P0-4 화면 교체 때 URL 자체를 숨기면 끝.
- `sendEmailVerification` 실패는 로그만 남고 사용자에겐 안 보인다 — 메일 미도착 민원 시 재발송 버튼이 필요(P0-4).

**최종 판정: 승인.** 차단 2건 요구대로 해결, 회귀·빌드·테스트 이상 없음.

검토 결과: 통과 가능
